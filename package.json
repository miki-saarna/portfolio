{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Mikito Saarna's Portfolio Website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx concurrently 'npm run start:server' 'npm run start:client'",
    "start:server": "npm --prefix ./server run start:dev",
    "start:client": "npm --prefix ./client start"
  },
  "keywords": [],
  "author": "Mikito Saarna",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
